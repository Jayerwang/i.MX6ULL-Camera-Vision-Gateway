在 Ubuntu 中打开终端，直接进到这个源码所在的 app 目录：
cd /home/book/Videos/cam-1-capture10frames-main/imx6ull_camera_project/app/


# 1. 编译 (如果你之前还没生成最新的文件)
make CC=arm-buildroot-linux-gnueabihf-gcc

# 2. 推送到开发板 (注意开发板连上 adb 了)
sudo adb push ov5640_capture /root/

# 3. 进板子运行
sudo adb shell
cd /root/
chmod +x ov5640_capture


./ov5640_capture -w 640 -h 480 -n 10 -o test10.yuv

ls -lh test10.yuv


# 在电脑终端执行
adb pull /root/test10.yuv ./
cd /d D:\linux\rk3588_ai_gateway-master\cam-1-capture10frames-main
ffmpeg -s 640x480 -pix_fmt yuyv422 -i test10.yuv frame_%03d.jpg